Troque o link do botão "Saiba mais" na seção "O Escritório" (About.tsx) de `#contato` para a rota interna `/escritorio` usando o componente `<Link>` do TanStack Router. Mantenha o mesmo estilo visual (cores, ícone ArrowRight, classes CSS).